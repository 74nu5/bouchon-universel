image : microsoft/dotnet:latest
stages:
  - build
  - deploy_test

before_script:
  - 'dotnet restore'

build:
 stage: build
 script:
  - 'dotnet build'

deploy_test:
  stage: deploy_test
  script:
    - ls
  environment:
    name: test
    url: http://bouchon.dotnet4ever.ovh
  only:
  - develop
